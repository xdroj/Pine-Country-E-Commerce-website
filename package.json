{
  "name": "pine-country-steakhouse",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "serve": "npx serve ."
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  }
}
